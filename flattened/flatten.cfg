# flatten.cfg — generated and maintained by flatten.py.
# Hand-editable; keys are stable. Lines beginning with # are ignored.

[repo]
name = ZipRip-game
prefix = zgt

# Full relative directory -> its cumulative flattened prefix.
[folders]
. -> 
Docs -> dcs
platform -> plt
platform/backups -> plt-bkp
platform/cloudflare -> plt-clfl
platform/mariadb -> plt-mrdb
trace-server -> trcsrv
trace-server/deploy -> trcsrv-dply
trace-server/dply -> trcsrv-deploy
wordpress -> wdpr

# Files or folders you chose NOT to upload. Remove a line to reconsider.
[flattenignore]
flattened-manually
platform/backups/_secrets_env.example
platform/mariadb/_secrets_env.example
trace-server/deploy/.gitignore-snippet
